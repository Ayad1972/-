# نقل المحادثات إلى OneDrive

أداة تنسخ **نسخة آمنة** من المحادثات إلى مجلد ون درايف على ويندوز. الأصل يبقى في مكانه حتى لا تتوقف Cursor أو واتساب أو تليغرام.

## التشغيل السريع

1. ثبّت تطبيق **OneDrive** على ويندوز وسجّل الدخول حتى يظهر مجلد `OneDrive` في مستكشف الملفات.
2. انسخ مجلد المشروع إلى جهازك، أو شغّل من نفس المجلد.
3. انقر مرتين على:

```bat
copy_chats_to_onedrive.bat
```

4. اضغط Enter للنسخ (أو اكتب `لا` للإلغاء).

سيتم إنشاء مجلد مثل:

```text
OneDrive\المحادثات\2026-08-19_0840\
```

وبعد المزامنة تظهر الملفات على الويب: https://onedrive.live.com

## ماذا تُنسخ؟

| المصدر | ماذا يؤخذ |
|--------|-----------|
| Cursor | قواعد بيانات المحادثات (`state.vscdb`) + تصدير Markdown مقروء إن وُجد Python |
| واتساب سطح المكتب / المتجر | بيانات التطبيق إن وُجدت |
| تليغرام | مجلد `tdata` إن وُجد |
| تيمز | بيانات التطبيق إن وُجدت |
| ربط الهاتف | رسائل الجوال إن وُجدت |
| سطح المكتب / التحميلات | ملفات اسمها يحتوي محادثة أو chat أو واتساب |
| هذا المشروع | مجلد `conversations` (أرشيف جلسات العمل السابقة) |

الكاش والسجلات الثقيلة تُتخطى حتى لا يمتلئ ون درايف بلا فائدة.

## لماذا نسخ وليس قص؟

قص ملفات واتساب أو Cursor من مكانها الأصلي يوقف البرنامج. الأداة **تنسخ** إلى ون درايف وتترك الأصل.

إذا كان عندك ملفات تصدير جاهزة (مثل `.txt` من واتساب) وتريد نقلها بالكامل، انقلها يدوياً من مجلد الحفظ بعد النسخ، أو مرّرها كمجلد إضافي:

```bat
python copy_chats_to_onedrive.py --source "D:\مجلد_المحادثات" --yes
```

## بدون Python

`copy_chats_to_onedrive.bat` يستخدم Python إن وُجد، وإلا PowerShell.

لتصدير محادثات Cursor كملفات `.md` تُقرأ في وورد/المفكرة:

```bat
install_python.bat
copy_chats_to_onedrive.bat
```

## تجربة بدون كتابة

```bat
python copy_chats_to_onedrive.py --dry-run --yes
```

## ملاحظة عن البيئة السحابية

هذه البيئة لا تصل إلى OneDrive على جهازك. شغّل الملف على ويندوز بعد تسجيل الدخول إلى ون درايف.
