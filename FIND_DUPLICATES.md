# حصر الملفات والمجلدات المتطابقة 100%

المطلوب: معرفة كل **ملف متطابق 100%** وكل **مجلد متشابه 100%**، ثم الإبقاء على **الأحدث/الأخير** فقط.

## مهم

- هذه البيئة السحابية **لا ترى سطح مكتبك**. التشغيل يتم على جهاز Windows عندك.
- الوضع الأول = **تقرير فقط** (لا حذف).
- الوضع الثاني = نقل المكررات إلى مجلد حجر `_DUPLICATES_QUARANTINE` مع إبقاء الأحدث.

## التشغيل (بدون تعقيد)

1. ثبّت Python إن لزم: `install_python.bat`
2. ضع مجلد المشروع على الجهاز أو افتحه من GitHub.
3. انقر مرتين:

```bat
find_dupes.bat
```

4. افتح من سطح المكتب التقرير:
   - `duplicates_report_....txt`
   - `duplicates_report_....csv`
5. إذا التقرير صحيح، شغّل:

```bat
find_dupes.bat APPLY
```

## ماذا يُبقى؟

- في كل مجموعة ملفات متطابقة بالمحتوى (نفس hash): يُبقى **أحدث تاريخ تعديل**.
- في كل مجموعة مجلدات متطابقة شجرةً ومحتوىً: يُبقى **أحدث مجلد**.
- الباقي يُنقل إلى الحجر (يمكن استرجاعه يدوياً).

## فحص مجلد معيّن فقط

```bat
python find_exact_duplicates.py --roots "%USERPROFILE%\Desktop"
python find_exact_duplicates.py --roots "H:\" --apply
```
